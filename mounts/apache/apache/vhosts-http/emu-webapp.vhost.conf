<VirtualHost *:80>
  ServerName emu-webapp.${BASE_DOMAIN}

  RequestHeader set X-Forwarded-Proto https
  ProxyPreserveHost On
  Timeout 28800
  ProxyTimeout 28800

  # Matomo analytics — serve tracker JS and proxy collection endpoint
  # Uses obfuscated paths (/vt, /vt.js) to avoid ad-blocker filter lists
  Alias /matomo-tracker.js /var/www/html/matomo-tracker.js
  ProxyPass /matomo-tracker.js !
  ProxyPass        /vt.js  http://matomo/matomo.js
  ProxyPassReverse /vt.js  http://matomo/matomo.js
  ProxyPass        /vt     http://matomo/matomo.php
  ProxyPassReverse /vt     http://matomo/matomo.php

  # EMU-webApp proxy (VirtualHost-level so Matomo exclusions take precedence)
  ProxyPass         /file  http://emu-webapp-server:17890/file
  ProxyPassReverse  /file  http://emu-webapp-server:17890/file
  ProxyPass         /      http://emu-webapp/
  ProxyPassReverse  /      http://emu-webapp/

  <Location "/">
    Header unset x-frame-options
    Header unset Content-Security-Policy

    # WebSocket upgrade to emu-webapp-server
    RewriteEngine On
    RewriteCond %{HTTP:Upgrade} websocket [NC]
    RewriteCond %{HTTP:Connection} Upgrade [NC]
    RewriteRule ^/?(.*)$ ws://emu-webapp-server:17890/$1 [P,L,QSA]

    # Inject Matomo tracker script into HTML responses from EMU-webApp backend
    AddOutputFilterByType SUBSTITUTE text/html
    Substitute "s|</head>|<script src='/matomo-tracker.js' defer></script></head>|ni"
  </Location>

  <Proxy ws://emu-webapp:80/>
    ProxySet timeout=28800
  </Proxy>

  ServerAdmin support@humlab.umu.se

  ErrorLog ${APACHE_LOG_DIR}/emu-webapp-error.log
  CustomLog ${APACHE_LOG_DIR}/emu-webapp-access.log combined

</VirtualHost>
