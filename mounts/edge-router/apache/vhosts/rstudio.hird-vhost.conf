#Port 80 will never be hit while the master-edge-router is being used since it automatically forwards all traffic to port 8081 which goes directly to 443
<VirtualHost *:80>
  ServerName rstudio.${HIRD_DOMAIN_NAME}
  Redirect permanent / https://rstudio.${HIRD_DOMAIN_NAME}/
</VirtualHost>

<VirtualHost *:443>

  ServerName rstudio.${HIRD_DOMAIN_NAME}

  SSLProtocol All -SSLv2 -SSLv3
  SSLCompression Off
  SSLCipherSuite "EECDH+ECDSA+AESGCM EECDH+aRSA+AESGCM EECDH+ECDSA+SHA384 EECDH+ECDSA+SHA256 EECDH+aRSA+SHA384 EECDH+aRSA+SHA256 EECDH+AESGCM EECDH EDH+AESGCM EDH+aRSA HIGH !MEDIUM !LOW !aNULL !eNULL !L$
  SSLEngine On
  SSLProxyEngine On

  #SSLCertificateFile    /etc/letsencrypt/live/hird.humlab.umu.se/fullchain.pem
  #SSLCertificateKeyFile /etc/letsencrypt/live/hird.humlab.umu.se/privkey.pem

  SSLCertificateFile /etc/certs/${HIRD_DOMAIN_NAME}/cert.crt
  SSLCertificateKeyFile /etc/certs/${HIRD_DOMAIN_NAME}/cert.key




  ProxyRequests on
  RequestHeader set X-Forwarded-Proto "https"	
  ProxyPass / http://rstudio-router/
  #ProxyPassReverse / http://rstudio-router/
  ProxyPassReverse / https://rstudio.${HIRD_DOMAIN_NAME}/  

  RewriteEngine on
  RewriteCond %{HTTP:UPGRADE} ^WebSocket$ [NC]
  RewriteCond %{HTTP:CONNECTION} ^Upgrade$ [NC]
  RewriteRule .* ws://rstudio-router%{REQUEST_URI} [P]





  #ProxyPreserveHost On
  ##Proxy websockets
  #RewriteEngine on
  #RewriteCond ${HTTP:Upgrade} websocket [NC]
  #RewriteCond ${HTTP:Connection} upgrade [NC]
  #RewriteRule .* "ws://rstudio-router/$1" [P,L]

  #ProxyPass / http://rstudio-router/
  #ProxyPassReverse / http://rstudio.${HIRD_DOMAIN_NAME}/
  #ProxyRequests off

  #<Location /secure>
  #  AuthType shibboleth
  #  ShibRequestSetting requireSession 1
  #  #ShibRequireSession On
  #  Require valid-user
  #  #Options +ExecCGI
  #  #AddHandler cgi-script .cgi
  #  #ShibUseHeaders On
  #</Location>

  ServerAdmin support@humlab.umu.se

  ErrorLog ${APACHE_LOG_DIR}/rstudio-error.log
  CustomLog ${APACHE_LOG_DIR}/rstudio-access.log combined

</VirtualHost>

